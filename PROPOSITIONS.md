# Pushback, ideas, propositions

Spirit: still skip Circle for dapp fees, still L1 covenants, still PegLab-will-depeg, still 1 receipt = 1 locked sompi **when the output exists**. Filter: what the git and the chain actually do.

## Pushback

**Publishing is not shipping.** Thirty-six repos, ~0 stars, one desk. The empty box is unchanged:

> No STP timeout lock/claim/reclaim journal. No local URL that returns **x402 v2**, takes this wallet’s tKAS, and comes back 200 + txid.

Parker already journaled receipts. Luke already has an RC binding. PegLab already teaches depeg. Another GitHub will not fill that box.

**stillpay-mainnet is a loaded name.** Same engines, `kaspa:` prefix, **no wasm submitter**. A GitHub called mainnet reads as live commerce. The README is more honest than the title.

**The 1 tKAS journal is not stillpay.** It is receive→change because 1 sompi is dust. Quote `sompi: "1"` is ENGINE_SPEC. Say that on the tin.

**sixpack.wtf is right in the paragraph and wrong in the title.** “Final verdict bind this envelope” + personal domain + STP kaspa-x402 fork looks like “Kaspa has x402.” RC.1, testnet-10, CAIP PR open, `allowMainnet` is a boolean.

**Three KaChats is git sprawl.** Upstream is KaspaSilver. groks-wallet cannot log in (wrong signer).

**kns does not fail #234 as a covenant.** It fails a comment grep. Don’t advertise it as a hostile name script.

**Ishum refusing this wallet is correct.** The till is mainnet-shaped. kUSD/USDT chairs still fight the master-file principle this desk wrote.

**Do not genesis PegLab from groks-wallet and call it done.** The sponsor is already another whale. Depeg is the lesson, not a listing.

## Ideas that survive a hostile read

1. **Skip Circle for dapp *fees*.** Prepaid work (KIP-21 grams) is a unit. A gram is not a dollar and must not become a KCC-20 named GRAM. Next step is WorkCredit `consume()` on an own UTXO, or admit the jar is a tab.
2. **PegLab will depeg.** Best teaching object in the catalog. Tiny pool, admin oracle, public failure.
3. **1 receipt = 1 locked sompi** once storage mass allows. Do not re-litigate with tPEG.
4. **One x402 envelope: Luke’s.** Native KAS, TN10, mainnet blocked. Steal k402’s *lock*. Never call k402 “x402.”
5. **HTTP 402 ≠ x402.** stillpay-quote-v1, kns 402, Gramlane header stay in their lanes.
6. **Desk holds 0 customer keys.** QR / `kaspa:` URI / paste txid. Inject = Kasware or Kastle.
7. **Unit of account ≠ settlement ≠ receipt.** EUR on the keypad, KAS on the chain. If Kaspa died tomorrow, the till should still make sense.
8. **#234 house rule.** Own-UTXO `validateOutputState` + `require(value)`. Timeout `.sil` that does not lock output value is a skim trap.
9. **Two Kaspas on one desk.** 16111 vs 16211. Mixing them is how you lose coins.
10. **DAGKnight outsider tests.** Help core without LARPing consensus.
11. **kascade review honesty.** Metering real; CDN not the git. Apply that standard to STP READMEs.
12. **kns-spec two-object honesty.** Indexer FCFS inscription is not a Toccata name UTXO.
13. **Anyone hosts.** Many jars ≠ one gram ledger until L1 consume().
14. **No seed in the page.** Already law.

## Merge / archive

| Action | Repos |
| --- | --- |
| Delete/archive | `ok` |
| Redirect → grok-heavy-test | `402-is-not-x402`, `x402-vs-grok`, `x402-ishum` |
| Freeze into master-file | `delusional-stp-grok-mix`, `project-delusional` (fix v1-rc1 row) |
| Archive | `kaspa-till` (Ishum replaced it), `stillpay-mainnet` until covenant journal, `kaachat-desktop` + `kachat-test-with-silver` as extra products |
| Tracking fork only | STP `kaspa-x402`, STP `rusty-kaspa` |
| Gist / elsewhere | `feedback-stp-delusional` |
| Keep as **host**, not second source | sixpack.wtf |

| Into | From |
| --- | --- |
| one `stillpay` (`NETWORK=`) | stillpay-tn10 + stillpay-mainnet + peglab-poc engines |
| ishum | kaspa-till, sixpack till tab |
| stp-kachat | the other two KaChats |
| kaspa-master-file | mix “orders” |

## One TN10 classroom path (do in order)

0. **Wallet:** groks-wallet. Address locked. Ports 16211. Seed off git. Mainnet 16111 left alone.
1. **Pins:** silverc **v1.0.0** `3ed9733`, rusty **v2.0.1**, TN10 not TN12, elldeeone kaspa-x402 **RC.1**. Recheck peglab-stp / project-delusional off v1-rc1.
2. **Receipt:** stillpay. Stop advertising 1 sompi as constructible. Journal **storage-mass-safe** lock / claim / reclaim of the timeout `.sil` with api-tn10 links. `64057dd7…` is a warm-up, not SCRIPT_ENFORCED.
3. **Depeg:** peglab-stp genesis from **its sponsor key**, or retarget a new series at groks-wallet. Run depeg lab. Never list tPEG as money.
4. **402 that charges:** clone **elldeeone/kaspa-x402**. One local URL → 402 → pay groks-wallet → 200 + txid. Native KAS. Not v1. Not mainnet.
5. **Till:** Ishum. Default **KAS only**. For classroom, add explicit `kaspatest:` store mode **or** stop pretending this wallet can check out coffee.
6. **Names:** kns-spec. Fix the grep test. One name that opens a paid local dApp without a seed.
7. **Inject:** wallet-integration. CLI seed is for scripts.
8. **Ops:** windows-p2p-node-guide + Xai.Kaspa.node + groks-wallet START-TN10. Three rituals, two networks.

**Kill-if shipped as product:** tPEG as money · GRAM as KCC-20 · fourth 402 envelope · TN12 · seed-paste UX · `allowMainnet` on kaspa-x402 · kUSD with no capital · “Kaspa has x402” because sixpack.wtf said bind.
